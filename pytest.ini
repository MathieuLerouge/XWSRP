[pytest]
testpaths = tests
markers =
    parity: many-sample tailored-vs-neighborhood pipeline parity checks, slow to run; exclude with `pytest -m "not parity"`
    llm: needs a running LLM backend (a local Ollama server, or a paid provider's API key), and is non-deterministic; exclude with `pytest -m "not llm"`
